meta {
  name: create role
  type: http
  seq: 3
}

post {
  url: {{base_url}}roles
  body: json
  auth: inherit
}

body:json {
  {
    "name": "rol de prueba"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
