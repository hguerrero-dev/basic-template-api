--- app/Modules/Roles/Services/RoleService.php
+++ app/Modules/Roles/Services/RoleService.php
@@ -16,7 +16,7 @@
 {
     public function getAll(?string $search = null, ?int $perPage = null)
     {
-        $page = request()->input('page', 1);
+        $page = \Illuminate\Pagination\Paginator::resolveCurrentPage('page') ?: 1;
         $perPage = $perPage ?? config('api.pagination.default', 15);
 
         $cacheKey = sprintf(
@@ -27,12 +27,24 @@
             $page
         );
 
-        return Cache::tags([Role::CACHE_TAG])->remember($cacheKey, 3600, functio
-n () use ($search, $perPage) {                                                              return $this->paginate(Role::with('permissions'), [
+        $cachedData = Cache::tags([Role::CACHE_TAG])->remember($cacheKey, 3600, function () use ($search, $perPage) {
+            $paginator = $this->paginate(Role::with('permissions'), [
                 'search' => $search,
                 'perPage' => $perPage,
                 'searchFields' => ['name']
             ]);
+
+            return [
+                'items' => $paginator->items(),
+                'total' => $paginator->total()
+            ];
         });
+
+        return new \Illuminate\Pagination\LengthAwarePaginator(
+            $cachedData['items'],
+            $cachedData['total'],
+            $perPage,
+            $page,
+            ['path' => \Illuminate\Pagination\Paginator::resolveCurrentPath(), 'pageName' => 'page']
+        );
     }
 
     public function getByOne($id)
